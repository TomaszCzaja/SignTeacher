<UserControl x:Class="SignTeacher.UI.View.ModelTrainerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:enum="clr-namespace:SignTeacher.Model.Enum;assembly=SignTeacher.Model"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="7" Orientation="Horizontal">
            <Button Content="Fist" Width="55" Margin="10" 
                    Command="{Binding ChooseClassCommand}" 
                    CommandParameter="{x:Static enum:OutputClass.Fist}"/>
            <Button Content="FullHand" Width="50" Margin="10" 
                    Command="{Binding ChooseClassCommand}" 
                    CommandParameter="{x:Static enum:OutputClass.FullHand}"/>
            <Button Content="Peace" Width="50" Margin="10" 
                    Command="{Binding ChooseClassCommand}" 
                    CommandParameter="{x:Static enum:OutputClass.Peace}"/>
            <Button Content="Save" Width="50" Margin="10" 
                    Command="{Binding SaveDataSet}"/>
        </StackPanel>
    </Grid>
</UserControl>
